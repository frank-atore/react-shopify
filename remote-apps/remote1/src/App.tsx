import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

const Remote1 = () => (
  <Card>
    <CardContent>
      <Typography variant="h5">Remote App 1</Typography>
      <Typography variant="body1">This is a federated module.</Typography>
    </CardContent>
  </Card>
);

export default Remote1;
